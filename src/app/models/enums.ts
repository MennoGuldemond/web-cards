export enum CardType {
  ship = 'Ship',
  support = 'Support',
  economic = 'Economic',
}

export enum CardRarity {
  common = 'Common',
  uncommon = 'Uncommon',
  rare = 'Rare',
}

export enum Effects {
  initiative = 'Initiative',
  health = 'Health',
  stealth = 'Stealth',
  dodge = 'Dodge',
}
