export enum CardType {
  ship = 'Ship',
  support = 'Support',
}

export enum CardRarity {
  common = 'Common',
  uncommon = 'Uncommon',
  rare = 'Rare',
}

export enum Ability {
  stealth = 'Stealth',
}
