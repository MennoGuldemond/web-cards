export enum CardType {
  ship = 'Ship',
  support = 'Support',
  economic = 'Economic',
}

export enum CardRarity {
  common = 'Common',
  uncommon = 'Uncommon',
  rare = 'Rare',
}

export enum Ability {
  stealth = 'Stealth',
}
